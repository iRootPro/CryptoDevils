type typeRoutes = {
    main: string;
    portfolio: string;
    watchlist: string;
    coin: string;
};

const ROUTES: typeRoutes = {
    main: '/',
    portfolio: '/portfolio',
    watchlist: '/watchlist',
    coin: '/coin',
};

export default ROUTES;
